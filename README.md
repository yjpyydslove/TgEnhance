# TgEnhance

Telegram 客户端的本地增强模块，基于 **LSPosed / Xposed**，不改动 Telegram 安装包本体，运行时挂载。

所有代码在 GitHub Actions 上云端编译，本地无需 Android SDK。

---

## 功能

按模块设置界面里的分组顺序排列，每一项都能单独开关。

### 多账号

| 项 | 说明 |
|---|---|
| 账号上限提升 | Telegram 原生限制为免费 3 个、会员 5 个账号。开启后可自行设置 3 – 16 个 |

实现上需要三处同时处理，只改一处无效（详见下方「实现要点」）。

### 界面与主题

| 项 | 说明 |
|---|---|
| 在 Telegram 设置里添加入口 | 在 Telegram 自己的设置页底部加一行「模块设置」，点击直接跳回本模块 —— 桌面图标隐藏后尤其有用 |
| 使用系统字体 | 把 Telegram 内嵌的 Roboto 替换为系统字体，代码块仍保留等宽 |
| 隐藏 Stories | 收起聊天列表顶部的 Stories 环与相关入口 |

### 网络

| 项 | 说明 |
|---|---|
| 阻止代理连通性探测 | Telegram 添加代理前会先发一次**不走代理**的探测请求，可被恶意代理链接用来套取真实出口 IP。开启后跳过该探测 |
| 阻止媒体自动下载 | 收到的媒体不再自动下载，只在手动点击时才下 —— 省流量 |
| 禁用自动播放 | 聊天里的 GIF 与视频不再自动播放，省流量也更省电 |

### 隐私与本地增强

| 项 | 说明 |
|---|---|
| 隐藏「正在输入 / 录音中」 | 不再向对方发送实时输入状态 |
| 防撤回 | 拦截服务器下发的删除请求，对方撤回的消息保留在你的记录里 |
| 不上报已读回执 | 读完消息不向服务器发送已读位置，对方看不到你的已读状态 |
| 隐藏在线状态 | 不上报「我在线」，对方看到你一直离线 |
| 隐藏对方的在线状态 | 反过来：对方明明在线，你这边也只显示「最近上线」 |
| 隐藏对方的最后上线时间 | 对方的状态整体显示为「很久以前」，不透露最后上线于何时 |
| 隐藏手机号 | 资料页里的手机号只保留末 4 位 |

### 广告屏蔽

| 项 | 说明 |
|---|---|
| 屏蔽赞助消息 | 聊天列表与频道内的推广内容不再请求、不再显示 |

默认关闭，开启前会说明「这会减少 Telegram 从你这里获得的广告收入」——
这是本模块唯一一项与收入相关的功能，因此刻意做得显眼。

### 高级功能

| 项 | 说明 |
|---|---|
| 强制平板布局 | 让手机也用上 Telegram 的平板界面（左右分栏） |
| 关闭更新提示 | 不再提示有新版本可用 |

这一组在设置界面里**默认收起** —— 普通用户用不上，误开后也较难自己排查。

### 反检测

| 项 | 说明 |
|---|---|
| 隐藏模块痕迹 | 让宿主应用看不出自己跑在 Xposed 上（详见下表） |
| 隐藏模块桌面图标 | 桌面不再显示本模块图标，Telegram 不受影响 |

「隐藏模块痕迹」覆盖的探测面：

| 探测手法 | 处理 |
|---|---|
| `Class.forName` / `loadClass` 查框架类 | 直接抛 `ClassNotFoundException` |
| `Throwable` / `Thread.getStackTrace()` 取栈 | 剔除框架帧后再返回 |
| `Thread.getAllStackTraces()` 全线程扫栈 | 逐线程剔除框架帧 |
| `printStackTrace` 输出里的框架帧 | 在取栈处剔除（它不经过 `getStackTrace`） |
| `getPackageInfo` 查模块包名 | 抛 `NameNotFoundException` |
| `getApplicationInfo` 读模块 meta-data | 同上 —— meta-data 是「这是 Xposed 模块」最直接的证据 |
| `getInstalledPackages` / `getInstalledApplications` 列全部应用 | 从结果里滤掉模块条目 |
| `Method.getModifiers()` 找 native 标志 | 只对被本模块 hook 过的方法修正 |

开启「隐藏模块痕迹」后，**自检报告里会出现一行反检测验证结果** ——
用上述同样的手法反过来探测自己，有任何一项没挡住都会点名列出来。
不需要你去猜它到底有没有生效。

只处理**本模块自己的痕迹**，不伪造 `Build` 属性、不隐藏 root、不动 `/proc/self/maps` ——
那属于全局环境改造，不该由一个 Telegram 增强模块代劳。
「抹除 native 标志」也只针对本模块 hook 过的方法，不做全量过滤 ——
全量过滤会把 Telegram 自己真正的 native 方法也伪装成 Java 方法，反而制造出新的异常特征。

---

## 配置即时生效

v2.0.0 起，所有 Hook 都**常驻挂载**，开关移到回调里实时读取。配合 Telegram
主界面 `onResume` 时的配置重载，**改完设置切回 Telegram 就生效，不用重启**。

只有少数依赖启动期状态的项例外（例如账号数组已经分配过的容量），这类改动
仍需重启 Telegram。

---

## 运行状态

判断功能是否**真的生效**，不必再去翻 LSPosed 日志 —— 设置界面底部有「运行状态」卡片：

```
报告时间 21:45:03
（Telegram 每次切换前后台都会刷新）

● 账号上限查询  已触发 12 次
● 配置热更新  已触发 4 次
○ 拦截撤回  未触发
...
```

原理：hook 端（Telegram 进程）把触发计数通过**显式广播**回传给设置界面，
令牌校验来源后落盘展示。之所以用广播，是因为两个进程不同 uid ——
模块私有目录 Telegram 没有权限，而 LSPosed 暴露的 `XSharedPreferences` 是只读的。

**怎么读这张表**

| 现象 | 含义 |
|---|---|
| 有数据、多数项已触发 | 模块工作正常 |
| 某功能开着但该项长期「未触发」 | 操作时没走到该路径，或 Hook 点已随版本失效 |
| 完全收不到报告 | LSPosed 里模块未启用 / 作用域没勾 Telegram |

「未触发」只说明这段时间没出现过对应动作（例如没人撤回消息），**不等于功能失效**。

---

## 风险提示

带副作用的功能在设置界面**开启时**会弹窗说明并要求确认，不会静默生效：

- **防撤回**：副作用是你自己也删不掉「为所有人删除」的消息；同时保留他人撤回内容可能涉及隐私与取证合规，请仅用于个人设备上的正当用途。
- **隐藏正在输入**：对方将完全看不到你的输入状态，会影响沟通预期。
- **不上报已读回执**：对方永远看不到你读过消息，群里的已读人数也不会增加；服务器侧仍认为这些消息未读，换设备或重新登录时未读标记可能重新出现，频道/群组未读计数会在服务器侧累积。
- **账号上限提升**：会扩容 Telegram 内部账号实例数组，少数版本上超出原生上限的账号可能不稳定，建议先保持默认值 6。

---

## 合规边界

本模块的定位是**本地体验增强**。其中有一项涉及收入，单独说明。

### 广告屏蔽（唯一一项与收入相关的功能）

- **默认关闭**，开启前弹窗说明「这会减少 Telegram 从你这里获得的广告收入」
- 实现方式是**客户端不再请求**赞助内容（`getSponsoredMessages` 直接短路），
  不是伪装、也不是劫持响应
- 只处理聊天列表与频道内的赞助消息，不做视频贴片等其他广告形式

这一项的存在感是刻意做明显的：它不是「顺手帮你屏蔽掉」，
而是「你明确知道并选择之后才生效」。

### 明确不做的事

- **不伪造 Telegram Premium** —— 不伪造会员状态、不本地解锁会员功能
- **不绕过内容保存 / 转发限制** —— 属于绕过内容保护
- **不阻止阅后即焚媒体被删除** —— 保留他人以为会消失的内容，伦理上不该由本模块代劳
- **不做批量群发、自动化推广**等可能被滥用的能力

---

## 设置界面

界面按 **Telegram 官方客户端**的观感重做，不是通用的 Material 设置页：

| 元素 | 对齐官方的做法 |
|---|---|
| 配色 | 浅色取官方 Day 主题主色 `#3390EC`；深色跟随官方 Night 主题的紫色 `#8774E1` |
| 分组 | 12dp 圆角卡片，左右各留 16dp；分组标题用主色，位于卡片上方 |
| 分割线 | 从文字左侧 16dp 起（不是通栏），颜色是 6% 前景色而非实色灰 |
| 开关 | 自绘控件 `TgSwitch`：滑块直径 28dp **大于**轨道高度 22dp，带投影，支持点击切换与横向拖拽 |
| 滑条 | 4dp 细轨道 + 圆形滑块，两端按滑块半径内缩 |
| 对话框 | 16dp 圆角卡片，按钮用主色 |

开关之所以不用系统 `Switch`：它的 thumb / track 尺寸关系在所有 ROM 上是固定的，改不出 TG 「滑块外扩」的效果，且不同厂商 ROM 的默认外观差异极大。

深色模式跟随系统，切换时界面会自动重建以刷新取色。

功能变多之后，顶部固定了一个搜索框（按标题与说明即时过滤，命中的分组之外整组隐去），
点击分组标题可以折叠该组 —— 折叠只收起卡片、保留标题，方便点回去展开。
「高级功能」与「诊断」两组**默认收起**（日用版的定位是装上就忘掉它），
点开一次后会被记下来，之后一直保持展开。
搜索与「只看已开启」时无视折叠状态 —— 否则会出现「搜到了结果、却因为它恰好在收起的分组里而看不见」。

首次打开设置页会弹一次引导，把「日用配置」真正写进配置文件
（详见 N1 的版本说明：默认值只写在注册表里的话，hook 端读到的仍是「未设置」= 关闭）。

**入口不止桌面图标一个。**开启「在 Telegram 设置里添加入口」后，
Telegram 自己的设置页最下面会多出一行「模块设置」，点一下就直接跳回本模块。
这一行是**额外**加的，不占用也不改动 Telegram 原有的任何条目 ——
所以就算哪天这项功能在你的客户端上失效，也只是这一行不出现，设置页本身一切照旧。

---

## 实现要点

### 功能注册表（v3.0.0）

「配置 key / 界面标题 / 说明文案 / 风险等级 / 关闭全部功能是否包含它」原本分散在
`Prefs`、`SettingsActivity`、`HookStats.expect` 三处，新增一个功能要改五六个文件，
漏一处就会出两种典型故障：**开关点了没反应**（key 没进配置清单）、
或**界面直接显示 `privacy.foo` 这种原始 key**。

现在集中声明在 `core/FeatureSpec.kt` 的一张表里：

```kotlin
FeatureSpec(
    key = Prefs.BLOCK_READ_RECEIPT,
    group = FeatureGroup.PRIVACY,
    title = "不上报已读回执",
    summary = "读完消息不向服务器发送已读位置，对方看不到你的已读状态。",
    risk = RiskLevel.HIGH,
    riskMessage = "开启后…",
)
```

界面、内存快照的 key 清单、导入配置时的风险确认名单、「关闭全部功能」的范围
全部由它派生，**结构上不可能不一致**。

新增功能只需两步：注册表加一条 + 在对应 `*Hooks` 里加 hook 点。

### 为什么不能只改 `MAX_ACCOUNT_COUNT`

Telegram 的账号体系是一组同构单例：

```java
private static volatile Xxx[] Instance = new Xxx[MAX_ACCOUNT_COUNT];
public static Xxx getInstance(int num) { ... Instance[num] ... }
```

而

```java
public final static int MAX_ACCOUNT_COUNT = 4;
```

是**编译期常量**，会被内联进每一处引用 —— 反射改字段值对方法体里早已写死的 `4` 毫无作用。

真正的上限由两处共同决定：

1. UI 层 `UserConfig.getMaxAccountCount()`（免费 3 / 会员 5）
2. 内存层各 `Instance[]` 数组的容量

因此本模块三管齐下：接管 `getMaxAccountCount()`、对每个管理器类的 `getInstance(int)` 挂前置钩子按需扩容数组、重写被常量写死循环上界的统计方法。

扩容采用「按需」而非「启动即扩容」：类以 `Class.forName(name, false, loader)` 加载（**不触发静态初始化**），只有真的用到第 N 个账号时才动数组 —— 避免启动期强行拉起整条 Manager 链。

### Hook 点依据

所有 Hook 点均取自 Telegram 官方源码（`DrKLO/Telegram`），而非猜测。
下表是**逐条核对过**的结果（v N1.14）：把项目里每个 `HookFinder.match`
的过滤条件，与源码里的真实签名比对了一遍。

| Hook 点 | 源码位置 | 过滤条件 | 核对 |
|---|---|---|---|
| `UserConfig.getMaxAccountCount()` | `UserConfig.java:124` | 返回 `int` | ✅ |
| `UserConfig.getActivatedAccountsCount()` | `UserConfig.java:101` | 返回 `int` | ✅ |
| `UserConfig.hasPremiumOnAccounts()` | `UserConfig.java:115` | 返回 `boolean` | ✅ |
| 各 `getInstance(int)` / `Instance[]` | `AccountInstance.java` 等 | — | — |
| `MessagesController.sendTyping(...)` | `MessagesController.java:11381 / 11385` | 返回 `boolean`，参数 ≥3 | ✅ |
| `MessagesController.deleteMessages(...)` | `MessagesController.java:9307+`（4 个重载） | 参数 ≥2 | ✅ |
| `MessagesController.completeReadTask(ReadTask)` | `MessagesController.java:14559`（**private**） | — | ✅ |
| `MessagesController.updateTimerProc()` | `MessagesController.java:10520` | — | ✅ |
| `MessagesController.getSponsoredMessages(long)` | `MessagesController.java:21612` | — | ✅ |
| `ConnectionsManager.checkProxy(...)` | `ConnectionsManager.java:739` | — | ✅ |
| `AndroidUtilities.getTypeface(String)` | `AndroidUtilities.java:2393` | — | ✅ |
| `AndroidUtilities.isTabletForce()` / `isTabletInternal()` | `AndroidUtilities.java:2948 / 2952` | 返回 `boolean` | ✅ |
| `SharedConfig.isAutoplayVideo()` / `isAutoplayGifs()` | `SharedConfig.java:740 / 744` | 返回 `boolean` | ✅ |
| `SharedConfig.isAppUpdateAvailable()` | `SharedConfig.java:777` | 返回 `boolean` | ✅ |
| `DownloadController.canDownloadMedia(...)` | `DownloadController.java:609 / 627` | — | ✅ |
| `StoriesController.has*()` | `StoriesController.java:254 / 287 / 1076 / 1342 / 1754` | 返回 `boolean` | ✅ |
| `LocaleController.formatUserStatus(...)` | `LocaleController.java:2888` | — | — |
| `PhoneFormat.format(String)` | `PhoneFormat.java:187` | 返回 `String`，1 参 | ✅ |

**两处值得单独记下的：**

- `ConnectionsManager.checkProxy(...)` 返回 **`long`**，所以回调里写的是
  `param.result = 0L`。若写成 `0`（`Int`）会抛 `ClassCastException` ——
  这类错误只在真机上炸，静态检查看不出来。
- `MessagesController.completeReadTask(ReadTask)` 是 **private** 方法。
  项目的 `hookAllByName` 按名字挂载所有重载、**不看可见性**，所以能挂上；
  但按「public 方法」去找会一直找不到它。

**没打勾的两行**是这次没逐条核的（`AccountInstance` 那组是数组扩容、
`formatUserStatus` 项目本身没加过滤条件），它们的存在性已由挂载期自检覆盖。

---

## 构建

推送到 `main` 自动编译并上传 artifact；打 `v*` 标签会额外发布 Release。

```bash
git tag v1.0.0 && git push origin v1.0.0
```

版本号在 `gradle.properties` 的 `VERSION_NAME` / `VERSION_CODE` 统一维护。

---

## 安装

1. 设备需已 root 并安装 LSPosed（Zygisk 版）
2. 安装 Release 中的 APK
3. 在 LSPosed 管理器中启用本模块，作用域勾选 Telegram
4. 打开模块图标进入设置界面调整功能
5. 从最近任务中划掉 Telegram 再重新打开（无需重启手机）

---

## 快速上手

装好之后**不需要逐项配置**：首次打开设置界面会弹一次引导，
选「应用日常配置」即可，之后正常用 Telegram 就行。

想自己调整时再往下看。

### 日用配置包含什么

| 分组 | 项 | 说明 |
|---|---|---|
| 多账号 | 启用多账号上限提升 | 上限提到 6，可在该组内用滑条调 |
| 界面与主题 | 启用界面定制 | 该组的总开关，具体项自行决定 |
| 界面与主题 | 在 Telegram 设置里添加入口 | Telegram 设置页底部多一行「模块设置」，点了跳回本模块 |
| 网络 | 启用网络增强 | 同上 |
| 高级功能 | 关闭更新提示 | 不再弹新版本提示 |
| 高级功能 | 详细日志 | 排查问题时才有用 |
| 高级功能 | 隐藏模块痕迹 | 挡掉宿主对 Xposed 的探测 |
| 诊断 | 输出诊断日志 | 挂载自检，反馈问题时用得上 |

**不包含**：隐私与本地增强整组、广告屏蔽整组，
以及「使用系统字体」「隐藏 Stories」这类纯外观偏好。

### 想逐项验证 Hook 是否生效

1. 打开 LSPosed 管理器 → 日志，搜索 `TgEnhance`，
   确认出现 `RESULT [界面] ...` 挂载日志。
2. 随便用一用 Telegram，回到模块设置界面看「运行状态」卡片 ——
   这里会列出每个 Hook 点被触发了多少次。
3. 出现「首次触发」说明该 Hook **确实作用在活跃路径上**。
   只有挂载日志、始终没有首次触发，则说明 Hook 点已随版本变动失效。

**日志里三类关键字：**

| 关键字 | 含义 |
|---|---|
| `RESULT [诊断]` | 挂载期探测：目标类 / 方法是否存在 |
| `RESULT [首次触发]` | 运行期：该 Hook 第一次被真正调用 |
| `RESULT [统计]` | 启动 90 秒后的汇总计数 |

---

## 反馈问题

提供以下四项基本可以一次定位：

1. **日志**：LSPosed 管理器 → 日志，搜索 `TgEnhance`，从 `===== TgEnhance =====` 起整段复制
2. **Telegram 版本号**：日志第一行的 `已挂载到 xxx (版本)` 里就有
3. **模块版本**：设置界面底部
4. **具体现象**：哪个开关、做了什么操作、期望什么、实际什么

有了 1 和 2，Hook 点要不要重新定位就是确定的事，不必靠猜。

---

## 诊断与版本适配

Hook 点依赖 Telegram 的类名与方法签名，这些会随版本变动而失效。为让适配不依赖反复试错，模块提供**两层可观测性**：

**1. 挂载期探测** —— 启动时主动探测所有关键类与方法是否存在，输出签名命中情况与各功能组的挂载结果。

**2. 运行期触发计数** —— 仅仅「挂上了」不等于「被调用了」。版本不匹配时最隐蔽的失败形态是：类名签名全对、挂载日志一切正常，但实际调用点早已改走别的路径，模块看起来工作正常却毫无效果。因此模块会在每个 Hook 点**第一次被触及时立即输出一条 `RESULT [首次触发]`**，并在启动 **90 秒后**输出汇总计数，**计数为 0 即代表该功能未真正生效**，无需依赖现象描述。

查看方式：**LSPosed 管理器 → 日志**，搜索关键字 `TgEnhance`。

反馈问题时把 `诊断报告` 与 `Hook 触发统计` 两段日志一起贴出来，即可精确定位需要修正的 Hook 点。

### 已知的 Hook 点失效风险

按可靠性从高到低排列，便于出问题时优先怀疑：

| 功能 | Hook 点 | 风险 |
|---|---|---|
| 多账号 | `UserConfig.getMaxAccountCount()` + `Instance[]` 扩容 | 低（结构稳定） |
| 系统字体 | `AndroidUtilities.getTypeface()` | 低 |
| 阻止代理探测 | `ConnectionsManager.checkProxy()` | 低 |
| 隐藏输入状态 | `MessagesController.sendTyping()` | 中（重载可能增减） |
| 防撤回 | `MessagesController.deleteMessages()` | 中（重载有 4 层嵌套） |
| 隐藏 Stories | `StoriesController` 布尔查询 | 较高（类名/方法变动较频繁） |

`隐私` 两项默认关闭，且开启时弹窗确认；`Stories` 若日志显示 `ui.stories` 计数为 0，说明该类已变动，需要重新定位。

**官方改名之后会怎样。** 每个 Hook 点都有精确名单 + 特征兜底两层，
行为在 N1.3 之后是明确的：

- 精确名单有命中 → 用精确的，**不涉及任何其他方法**。
- 精确名单落空、有特征 → 兜底接住，并在自检里标为「靠特征命中」，
  提示这个名字已经和官方源码对不上。
- 精确名单落空、**没有特征**（强制平板、关闭更新提示、隐藏手机号这三项）→
  不做任何 hook，改为在「运行状态」里显示「当前客户端不支持」。
  这是有意为之：**宁可功能失效，也不要乱 hook 一批无关方法。**
  界面错乱比功能不可用难排查得多。

---

## 签名说明

仓库中的 `keystore/tgenhance.p12` 是**公开的自签名密钥**，用途仅是保证 CI 每次构建的签名一致，从而支持覆盖升级。

密码为 `tgenhance`。它没有任何保密价值，也不代表任何组织身份 —— **请勿用于正式发布或分发场景**。若要正式分发，请自行生成密钥并改用 CI Secrets 注入。

---

## 版本说明

每个版本做了什么、为什么这么做。

### v1.x —— 打地基

**v1.0.0** 功能落地：六项本地增强（多账号上限、系统字体、隐藏 Stories、代理探测防护、
隐藏输入状态、防撤回）+ 云端 CI 打通。
多账号是其中唯一的硬骨头 —— `MAX_ACCOUNT_COUNT` 是编译期常量会被内联，反射改它无效，
真正的上限由 `UserConfig.getMaxAccountCount()` 与各 `Instance[]` 数组容量共同决定，必须三管齐下。

**v1.1.0** 让效果可验证。加入 Hook 触发计数 —— 「挂上了」不等于「被调用了」，
版本不匹配时最隐蔽的失败形态就是挂载日志一切正常、实际毫无效果。
同时把防撤回从「拦截所有 `forAll` 删除」改为「调用栈 + `dialogId==0`」双判据，
消除对自己删消息的误伤。

**v1.2.0** 修掉一个会误导人的设计缺陷。原统计只在启动 45 秒后汇总一次，
用户在那之前没操作就会看到计数 0，把「还没触发」误读成「Hook 失效」。
改为每个 Hook 点首次触发立即输出。

**v1.3.0** 设置界面按 Telegram 官方客户端重做。配色取官方 Day(`#3390EC`) / Night(`#8774E1`) 主题；
自绘 `TgSwitch`（滑块 28dp 大于轨道 22dp、带投影）—— 系统 `Switch` 的 thumb/track
尺寸关系在所有 ROM 上固定，改不出这个效果。

### v2.x —— 可用性与诊断

**v2.0.0** 配置热更新。所有 Hook 改为常驻挂载、开关在回调里实时读取，
配合 `LaunchActivity.onResume` 时的配置重载，改完设置切回 Telegram 即生效。
新增「不上报已读回执」（接管 `completeReadTask()` —— 已读位置的唯一网络出口）
与 `HookFinder`（按返回类型 + 名字特征定位 Hook 点，抗官方改名）。

**v2.1.0** 运行状态回显。Hook 端把触发计数通过显式广播回传设置界面 ——
不再需要离开应用去翻 LSPosed 日志。

**v2.2.0** 快捷配置与导入导出。三组低风险预设；配置可导出成明文文本。
导入同样要过风险确认，否则贴一段文本就能绕过所有风险提示。

**v2.3.0** 深度自检面板。自检结果结构化成清单，命中的与未匹配的分开呈现。

**v2.4.0 / v2.5.0 / v2.5.1** v2.4.0 与 v2.5.0 的 CI 都停在同一个缺失的 import 上
（本机没有 Kotlin 编译器，这类错误只能等 CI 暴露）。修复后由 v2.5.1 一并承载：
新增隐藏在线状态、阻止媒体自动下载；配置改内存快照、回调加异常兜底、
客户端识别不再依赖包名（改过包名的 fork 也能用）。

### v3.x —— 工程化与反检测

**v3.0.0** 功能注册表。此前配置 key / 界面标题 / 风险等级分散在三个文件，新增功能要改五六处，
漏一处就会出现「开关点了没反应」或界面显示 `privacy.foo` 这种原始 key。
现在全部从 `core/FeatureSpec.kt` 一张表派生，结构上不可能不一致。

**v3.1.0** 反检测。挡掉三种最常见的检测手法：类名探测（`loadClass` 直接拒绝）、
堆栈探测（`getStackTrace` 剔除框架帧）、包名探测（`getPackageInfo` 报未安装）。
刻意不做伪造 `Build` 属性 / 隐藏 root —— 那是全局环境改造，不属于本模块职责。

**v3.2.0** 搜索与折叠，应对功能变多后「找一项要一路滑下去」。

**v3.3.0** 健壮性收尾。12 个 Hook 回调全部有异常兜底，异常在日志里带模块前缀，
便于给「宿主行为异常」这类问题归因。

**v3.4.0** 禁用自动播放（`SharedConfig.isAutoplay*()`），并修掉 `XC_MethodReplacement`
回退路径的拆箱隐患：`invokeOriginal()` 返回 `null` 时给 boolean 方法会抛 NPE ——
典型的「开关关着却反而崩了」。

**v3.5.0** 自检给出候选方法名。官方把 `hasStories` 改成 `hasNewStories` 时，
自检会直接列出同类里名字相近的方法，不必再靠反复跑 CI 试错。

**v3.6.0** 诊断报告一键分享（系统分享面板，零权限）；
顺带删掉从未生效的 `NET_TIMEOUT_SCALE` —— 连接超时在 native 层，Java 侧改不了。

**v3.7.0** 配置格式版本化（`TGE2:`，兼容 `TGE1:`），导入加长度上限与严格取值。

**v3.8.0** 挂载耗时统计。模块在 `handleLoadPackage` 里同步执行，耗时直接算进 Telegram 启动时间；
逐组输出耗时之后，才谈得上判断某个 Hook 值不值得重新实现。

**v3.9.0** 配置 key 一致性自检（找出已无对应功能的残留项）；重置不再清掉回传令牌。

### v4.0.0

文档收口与最终一致性核对。功能层面与 v3.9.0 一致 —— 这是一个「确认可用」的版本，
不是又一个新功能版本。

### v4.x —— 反检测深化与自检补全

**v4.1.0** 反检测再进一步。Xposed 挂载方法时会把它的实现换成 native stub，
于是 `Modifier.isNative()` 从 false 变成 true —— 这比类名 / 堆栈 / 包名三种探测都可靠。
新增方法登记与 `Method.getModifiers()` 拦截，**只抹掉自己 hook 过的方法**的 native 标志，
不做全量过滤（全量会把 Telegram 真正的 native 方法伪装成 Java 方法，反而制造新特征）。
同时新增 `HookInstaller` 作为 hook 安装的统一入口，确保没有路径绕过登记。

**v4.2.0** 「只看已开启」筛选与「已开启 N / 共 M 项」统计。

**v4.3.0** Hook 冲突检测。复用 v4.1.0 的原理：挂载前若目标方法**已经是** native，
说明它已被别的模块 hook 过。两个模块抢同一个方法时的表现极难排查
（互相覆盖导致其中一个静默失效，或因为优先级不同时灵时不灵），现在会直接点名。

**v4.4.0** 高频 Hook 预警。单点触发超过 1 万次时提示一次，并指出「卡顿就优先关它」；
汇总报告补充统计时长与高频清单。刻意没做逐回调计时 —— 收益只是诊断更细一层，
而代价是改动全部 12 处回调体。

**v4.5.0** 挂载结果汇总。六组 Hook 的结果汇总成一行结论，失败时明确列出是哪几组。

### v5.0.0

Hook 清单统一注册。统计 key 与它的中文名此前分散在两处：hook 端在 `HookEntry` 里
逐个 `expect(...)` 登记，界面端在 `HOOK_LABELS` 里再维护一份映射。
新增 Hook 点时忘掉任何一边，症状分别是「该项在运行状态里永不出现」和
「界面直接显示 `privacy.foo` 这种原始 key」。现在两边都从 `HookCatalog` 派生 ——
和 v3.0.0 的 `Features` 是同一个思路，只是下沉了一层。

至此「新增一个功能」从开头的「改六个文件」收敛成「改三处」：
`Features` 加一条声明、`HookCatalog` 加一条计数点、对应 `*Hooks` 里加 hook 点。

### v5.x —— 兼容性主线

**v5.1.0** 客户端识别与类路径候选解析。同一个类在不同 fork 里路径可能不同
（Telegram 自己就把 Stories 从 `org.telegram.messenger` 搬到了 `org.telegram.ui.Stories`），
写死路径的话换一个客户端就整组功能失效。改为维护候选列表逐个探测。

**v5.2.0** 框架差异兼容。免 root 方案（LSPatch 等）不提供跨进程配置映射、
也不走 Zygote 注入 —— 前者会让所有开关静默走默认值（等于全关）。
现在这两种情况都会明确报出来，自检里也多了一项「框架」。

**v5.3.0** 功能可用性上报。Hook 点在当前客户端不存在时，不再只是日志里
一行「未找到 XXX」，而是汇总进自检，直接告诉用户「这几个开关在你这版上不会生效」。

**v5.4.0** Android 15 edge-to-edge 适配。targetSdk 35 起系统强制全屏，
不处理的话标题会被状态栏盖住。

**v5.5.0** 判据去索引化 + 版本下限 + 挂载失败上报。防撤回的参数判断从
「记死索引」改为「按类型扫描」，避免官方调整签名后静默错位、甚至误拦用户自己删的消息。

**v5.6.0** 隐藏对方在线状态（`LocaleController.formatUserStatus`）、
包列表隐身（`getInstalledPackages` 过滤）。

**v5.7.0** 核心 hook 点加特征兜底：精确名 + 名字特征双轨匹配。

**v5.8.0** 借鉴现有开源模块的合规功能：隐藏手机号（遮罩保留末 4 位）、
强制平板布局、关闭更新提示；README 补「参考与致谢」，划清与它们的边界。

**v5.9.0** 广告屏蔽。接管 `MessagesController.getSponsoredMessages(long)` ——
它是赞助消息的唯一入口。在它执行前返回一个空壳 info，效果是**请求根本不发出去**、
界面拿到的列表永远是空的；比拦响应或隐藏 UI 都干净。
这一项默认关闭，且开启前明确告知「会减少 Telegram 的广告收入」，
是本模块唯一涉及收入的功能。README 的「合规边界」相应重写，
把「不做广告」改成「做，但这是你明确选择的」。

### v6.x —— 可观测性打磨与「高级功能」分区

**v6.0.0** 适配质量可观测。v5.7.0 加的特征兜底让抗改名能力变强，但也带来一个新盲区 ——
**接住了，但没人知道你差点没接住**。HookFinder 现在记账「靠特征而非精确名命中的方法」，
自检里单列出来，这是版本适配的早期预警。

**v6.1.0** 自检按严重度排序（有问题的排最前），运行状态卡片支持长按复制。

**v6.2.0** 分组折叠状态持久化 —— 切深色模式、旋转屏幕都会重建 Activity，不记住的话每次都要重收一遍。

**v6.3.0** 回调异常计入统计。此前异常只写进日志，二十多行里根本没人会发现；
现在它是「运行状态」里的一行计数。XLog 与 HookStats 之间用回调解耦，避免依赖成环。

**v6.4.0** 内部异常进自检：兜住了不等于没问题，它说明模块内部有逻辑在这台设备上跑不通。

**v6.5.0** 新增「高级功能」分区，把普通用户用不到的选项集中（阻止代理探测、强制平板、
关闭更新提示、隐藏模块痕迹），其中纯增益的两项预设为开启。

**v6.6.0** 快捷配置新增「推荐配置（新手用这个）」：四项纯本地显示类调整，开完就比默认好用。

**v6.7.0** 详细日志开关。关掉后只留 RESULT / WARN / ERROR —— 过程日志是给自己看的，
结论日志是给用户看的，后者任何时候都不该被挤掉。

**v6.8.0** 主 Activity 兜底发现：候选类名全没命中时，改为问系统要启动信息。
**候选列表本质是在猜，系统的启动信息是事实。**

**v6.9.0** 分组说明 —— 标题只有两三个字，普通用户未必判断得出里面装了什么。

### v7.x —— 兼容性深化

**v7.0.0** 底部信息补全：模块版本 + 当前客户端，截图反馈时一张图带齐关键信息。

**v7.1.0** 自检逐条标注「特征兜底命中」，不只是汇总一行。

**v7.2.0** HookFinder 支持参数个数范围（minParamCount / maxParamCount），应对重载增删。

**v7.3.0 / v7.4.0 / v7.4.1** 前两版的 CI 都因同一处结构破坏失败
（批量补丁把 `FeatureSpec(key = Prefs.HIDE_PHONE,` 两行吃掉了，只有 CI 能发现），
由 v7.4.1 一并承载。内容：隐藏对方的最后上线时间（文案用 TG 自己的字符串资源，
不硬编码中文）；自检耗时与输出量保护（挂载超 400ms 提示可关诊断日志，单个类输出截断到 8 条）。

**v7.5.0** 搜索支持英文关键词（ads / recall / font / tablet …）。

**v7.6.0** 不支持客户端的明确提示：Telegram X 是另一套架构；
包名像 TG 系但找不到核心类时会提示「检查 LSPosed 作用域」——
这是最常见的配置失误，此前只是安静地跳过。

**v7.7.0** 一键关闭有风险的功能：试用后想收敛回安全默认，又不想丢掉显示类调整。

**v7.8.0** 诊断报告补设备与系统环境 —— 很多 Hook 失效其实是特定 ROM 或系统版本的问题。

**v7.9.0** 修复批次：配置 key 清单改 lazy 缓存、底部客户端信息随回传刷新。

### v8.0.0

文档收口。功能与 v7.9.0 一致 —— 把版本说明补齐到当前，方便判断每个改动是哪一版引入的。

### N1 —— 日用版本

版本号从 8.0.0 跳到 N1。这一版不再加新功能，而是把整个模块**收敛成装上就能用的形态**。
这里的 N 不是「测试版」的意思，后面还有 N2、N9.9，再往后是 X 系 —— 它只是一条新的编号线。

**日用配置落到配置层，而不只是界面上看着开着。**
此前模块的默认值只写在 `FeatureSpec` 里，作用仅限于「界面显示成什么状态」；
hook 端读到「配置里没有这一项」时一律按 `false` 处理。
两者不一致的后果很具体：新用户装完打开设置页，看到一堆开关是开着的，
实际上一个都没生效 —— 他只会认为模块坏了，几乎不可能猜到是配置压根没落盘。
现在首次进入设置界面会弹一次引导，确认后把整份日用配置真正写进 `SharedPreferences`。

**日用配置选了什么，以及刻意没选什么。**
进日用配置的只有 7 项：多账号上限、界面定制、网络增强，
外加「关闭更新提示」「详细日志」「隐藏模块痕迹」「输出诊断日志」。
判定标准是两条 —— 零副作用，且不改变「对方能否感知到你」。
因此**隐私与本地增强整组、广告屏蔽整组都不在其中**：
它们会真的改变对方眼中的你（看不到输入状态、看不到已读、看不到在线），
这类取舍必须由用户自己看过说明后决定，不能替他先选好。
「使用系统字体」「隐藏 Stories」这类纯外观偏好同样不在其中 —— 它们是用户的选择，不是默认值。

**高级功能与诊断两组默认收起。**
日用版的定位是「装上就忘掉它」，而这两组里的东西（强制平板布局、阻止代理连通性探测、
详细日志开关…）普通用户既用不上，误开后也较难自己排查。
收起的只是**初始状态**：点开一次后会被记下来，之后一直保持展开，不会反复跟用户作对。

**顺手修掉的两个显示问题。**

- 分组说明文字此前是独立的一行、不归分组管，收起分组后会留下一句无主的说明悬在页面上。
  现在它跟着卡片一起收放。
- 搜索与「只看已开启」此前**不会**展开收起的分组 ——
  明明搜到了结果，却因为它恰好在收起的分组里而看不见。
  现在这两种状态下无视折叠，箭头也跟着显示成展开态，免得出现「箭头收起、内容露着」的矛盾画面。

---

### N1.1 —— 稳定性、安全与反检测

N1 把模块收敛成日用形态，这一版回过头补**底座**：跨 Android 版本的正确性、
模块自身的隐身、以及为「设置入口」做铺垫。

**修掉一个只在 Android 13 及以下出现的静默失效。**
设置界面注册诊断回传接收器时用的是带导出标志的三参数 `registerReceiver` ——
那个重载是 **Android 14 才有的**。在 Android 13 及以下调用会抛
`NoSuchMethodError`，而异常恰好被外层的 `catch (Throwable)` 吞掉，
结果是：**这些设备上的「运行状态」永远是空的**，用户只会看到
「尚未收到运行报告」，然后以为模块没挂上。
现在按系统版本分支：14 及以上传导出标志，13 及以下走旧式两参数注册。

**Android 版本差异集中到一个文件。**
新增 `core/OsCompat.kt`，把 14 / 15 / 16 / 17 之间会影响本模块的改动
（接收器导出标志、强制 edge-to-edge、隐式 intent 限制）全部收在那里，
每个分支只有一个地方需要读、需要改。诊断报告里也会把这些判定结果
直接写出来 —— 用户反馈时不必再去翻系统设置念版本号。

**反检测补三个此前没覆盖的探测面。**

| 新增 | 挡掉的探测手法 |
|---|---|
| `Thread.getAllStackTraces()` | 一次调用扫**所有线程**的栈找框架帧，比逐线程取高效得多，收获也更大 |
| `Throwable.getOurStackTrace()` | `printStackTrace` 输出里的框架帧。**它不经过 `getStackTrace()`** —— 检测方把异常输出重定向到内存流再搜关键字，此前完全挡不住 |
| `getApplicationInfo` | 直接读模块包的 meta-data，`xposedmodule` / `xposedminversion` 是「这是 Xposed 模块」最直接的证据，且不经过 `getPackageInfo` |

三项都只在真的剔掉了东西时才替换返回值，否则原样放行 ——
高频路径上不做无谓的对象分配。

**桌面图标可以隐藏了，且不会把自己锁在外面。**
新增「隐藏模块桌面图标」开关。实现上没有直接禁用设置页的 LAUNCHER 组件 ——
那样会把设置页一起停掉，连 LSPosed 管理器都打不开。
改为用一个 `activity-alias` 承载桌面图标，设置页本身另挂一个
不带 LAUNCHER 的显式 action（`com.yjp.tgenhance.action.SETTINGS`）。
隐藏的**只有那个 alias**，设置页与显式 action 完全不受影响。
每次打开设置页都会把系统里的实际组件状态与配置对一次，避免出现
「开关显示开着、图标其实还在」这种自相矛盾。

**为「设置入口注入 Telegram 设置页」做探测，但这一版还没注入。**
Telegram 设置页的列表构建方式在版本之间差别很大
（旧版用 `rowInfo`，新版用 `UItem` + `UniversalAdapter`），
贸然注入会在部分客户端上把设置页画错。所以这一版**只探测不插条目**：
自检里会告诉你当前客户端的设置页类叫什么、列表构建方法在不在，
将来做注入时不必再从零猜。

---

### N1.2 —— 反检测可验证

继续 N1.1 的主线。这一版的重点是：**把「隐身有没有生效」从只能相信变成可以验证**。

**在此之前，反检测是一个黑箱。**
开关显示开着，但挡住没挡住、挡住了几条，用户和我都看不到。
某条拦截因为宿主版本变化失效时，没人会知道 ——
直到某个客户端开始弹「检测到 Xposed」。

**现在用检测方的手法探测自己。** 五项探测，每项对应一种真实的检测手法：

| 探测 | 手法 |
|---|---|
| 按类名探测框架 | `Class.forName` 查 `de.robv.android.xposed.*` |
| 异常堆栈探测 | 造异常检查栈里有没有框架帧 |
| 已安装应用列表探测 | 列全部应用再按包名找可疑项 |
| 按包名探测模块 | 指名道姓查模块包，并读 `xposedmodule` 元数据 |
| native 标志探测 | 反射找「Java 方法却带 native 标志」的 |

结果进自检报告：全部挡住时不占行，**有任何一项漏掉都会点名列出来**。

**一个容易做错的地方。**「native 标志探测」不能用 `method.modifiers` 去验 ——
那是我们自己 hook 过的方法，读到的永远是抹平之后的假值，
测出来必然「通过」。所以这里绕过自身拦截，直接反射读 `Method` 内部的修饰符字段，
看到的是 ART 里的真实状态。读不到时**不假装通过**，而是显示为「无法验证」——
一个永远说「没问题」的自检比没有自检更危险。

**顺带修的两个隐患。**

- 《隐身登记失败》此前是彻底无声的：`markHooked` 抛异常被 `catch (Throwable)` 吞掉后，
  方法成功 hook 了却没进名单，于是 native 标志一直暴露 —— 检测方一眼就能看出。
  现在这个失败会记进计数，出现在「运行状态」里。
- 登记名单改为存 `WeakReference`。之前存的是签名字符串（本身没问题），
  但为了自检能抽查实际方法对象，需要保留 `Method` 引用 ——
  强引用会把每个被 hook 的方法连同它的 `Class` 一起钉在内存里，
  对一个随 Telegram 全程存活的模块来说那是实打实的泄漏。

---

### N1.3 —— 修掉一个会破坏界面的匹配缺陷

这一版只做一件事，但它比前几版任何一项都重要：
**修掉 Hook 点定位里「精确名单 + 特征兜底」的取并集缺陷。**

**问题是什么。** 模块定位 Hook 点用 `HookFinder.match(...)`，
它同时接受两样输入：一份**精确方法名名单**（保证不误伤），
和一组**特征条件**（名字前缀 / 子串，用于官方改名后还能接住）。
设计意图是「精确优先、特征兜底」，但实现上返回的是两者**并集**。

于是任何名字沾上特征的同类方法都会一起被 hook ——
哪怕它被刻意排除在外。

**最严重的一处：强制平板布局会把整个工具类改坏。**
这一项只传了精确名单和「返回 boolean」，
**没有传任何特征条件**。而 `findMethods` 在没有名字条件时不做名字过滤，
返回的是**该类里所有返回 boolean 的方法**。两者并起来的结果是：
开启「强制平板布局」后，`AndroidUtilities` 里每一个返回 boolean 的方法
都被强制成 `true` ——

```
isTablet          → true     isLandscape  → true
isSmallTablet     → true     isRTL        → true
hasSoftNavigation → true     isHuaweiStoreBuild → true
```

横竖屏判断、RTL 布局、导航栏检测全部失真。用户看到的是**界面明显错乱**，
而原因藏在一个跟界面毫无关系的方法匹配里。

同一形态还有两处：

| 位置 | 并集导致的误伤 |
|---|---|
| 防撤回 | `deleteMessagesByChatId` 也被挂上，它会调用 `deleteMessages`，成为**双重处理** |
| 隐藏手机号 | `PhoneFormat` 里所有单参数返回 String 的方法都被脱敏 |

**改法。** 把「并集」改成**二选一**：

- 精确名单**有命中** → 只返回精确的，特征完全不参与。误伤归零。
- 精确名单**全部落空** → 说明官方改名了，这时才启用特征兜底，
  并如实记账，自检报告里会提示「这个 Hook 点已靠特征勉强接住」。

**再加一道安全闸门。** 精确落空、并且**一个特征条件都没给**时，
直接返回空列表、不做任何 hook，而不是让 `findMethods` 返回全类方法当兜底。
宁可让功能走「当前客户端不支持」分支 —— 那是个明确的、用户看得懂的状态，
远比悄悄改坏半个工具类要好。

**顺带修掉一条死逻辑。**
自检报告里原本有一行「（特征兜底命中）」标注，用的是
`HookFinder.isFuzzyMatched(cls, 精确名单里的名字)`。
但在新语义下这个问法**恒为 false**：精确命中时根本不会记兜底账，
精确落空时前面已经走了「方法缺失」分支。
也就是说这行标注从来没显示过。删掉了 —— 判断「官方有没有改名」
应该看整体兜底清单，那才是准确的位置。

**验证方式。** 用同构逻辑复现了修复前后的行为，四个用例对照：

| 用例 | 修复前 | 修复后 |
|---|---|---|
| 强制平板（无特征条件） | hook 到 **9 个** boolean 方法 | **2 个**，精确名单内 |
| 防撤回 | 3 个（含 `deleteMessagesByChatId`） | **1 个**，精确 |
| 改名后 + 有特征 | 兜底生效 | 兜底生效（未受影响） |
| 改名后 + 无特征 | hook 到 **9 个**（灾难） | **0 个**，安全闸门拦下 |

---

### N1.4 —— 把设置入口注入 Telegram 设置页

从 N1.1 开始就在探测设置页结构，这一版真正把入口插进去了。

**为什么这件事拖了三个版本。** 往别人的列表里插条目，出错方式比「功能不生效」
严重得多：插错位置会打乱分组分割线，用错 viewType 会让 `RecyclerView`
在滚动时抛异常 —— 表现是**设置页打不开或直接崩**。而这跟「增强模块」
几乎联系不起来，用户根本不会往这边想。所以宁可先花一版把结构探清楚。

**先把结构查清楚，不猜。** 对着 Telegram 源码逐条核对了这几个关键点：

| 项 | 实际情况 |
|---|---|
| 列表怎么填 | `SettingsActivity.fillItems(ArrayList<UItem>, UniversalAdapter)` |
| 条目怎么造 | `UItem.asSettingsCell(id, icon, text, value)` |
| 分组标题 | `UItem.asHeader(text)` |
| 点击怎么走 | `SettingsActivity.onClick(UItem, View, int, float, float)`，内部 `switch (item.id)` |

**一个容易想当然的地方。** `UItem` 里有个 `clickCallback` 字段，看着像是
通用的点击回调 —— 但翻遍 `UniversalAdapter` 会发现它只在少数几种 viewType 下
被用到（搜索项的关闭按钮、可折叠项的箭头等），**设置条目这一种不看它**。
按「给条目挂个回调」的思路写，结果是点了完全没反应。

真正的做法是给条目一个**不会和 Telegram 撞车的 id**，再 hook `onClick`
按这个 id 认领。Telegram 自己的 `switch` 没有这个 case，会走 default
什么也不做 —— 所以这里**不需要阻断原方法**，两边天然不打架。
少一次对宿主逻辑的干预，就少一分跑偏的可能。

**风险怎么压下去的。** 每一处判断都按「拿不准就不做」写：

1. **只插自己的一行，绝不改动已有条目** —— 不改 id、不调顺序、不动颜色。
2. **两处结构判据**：靠 viewType 认出搜索模式（首项是 `asSpace`，此时列表里
   装的是搜索结果，插图会插到搜索结果中间），靠锚点 id 确认这是设置主页。
   任何一条对不上就整体放弃。
3. **全部反射 + 探测**：`UItem` 的工厂方法、`fillItems` / `onClick`
   都是探测式获取，某台设备上没有就跳过，不影响其余 Hook 组。
4. **重复调用保护**：`fillItems` 在设置页每次刷新都会跑，靠 id 去重。

**失败时会怎样。** 任一环节不满足，入口就不出现，同时在「运行状态」里
标为「当前客户端不支持」并写一行日志 —— 但**设置页本身完全不受影响**。
桌面图标（或其隐藏后的显式 action）始终是退路，这个入口只是多个便利。

**验证方式。** 和 N1.3 一样，本机没有编译器，用等价模型把插入逻辑复现出来，
五个场景 14 项断言全绿：

| 场景 | 期望 | 结果 |
|---|---|---|
| 正常设置主页 | 插入，位置在末尾版本号之前 | ✅ 尾部顺序 `TgEnhance → 模块设置 → versionView` |
| 搜索模式 | 不插入 | ✅ 列表保持原样 |
| 重复刷新 | 不重复插入 | ✅ 始终只有一份 |
| 陌生结构（无锚点） | 不插入 | ✅ 列表保持原样 |
| 无版本号视图 | 降级为追加到末尾 | ✅ 仍在末尾 |

---

### N1.5 —— 回调异常防护审计 + 自动化检查

这一版做的是**回头审计**：把所有 Hook 回调逐个过一遍，看异常防护有没有漏。

**为什么这件事值得单独做一版。** Xposed 框架自己会捕获回调异常并继续执行原方法，
所以回调抛异常**不会崩 Telegram** —— 但它的日志里看不出这是模块引起的。
模块自己再包一层 `guard` 的作用，是把「回调抛过异常」变成一条带模块前缀、
且计入 `internal.callbackError` 的记录，于是能直接在设置界面的
「运行状态」里看到。

漏包的回调不致命，但会让**归因**丢失：用户看到「有时功能不生效」，
日志里却什么都没有。

**审计结果：25 个回调，2 处真缺防护。** 都在写 `param.result`：

| 位置 | 为什么有风险 |
|---|---|
| `UserConfig.getMaxAccountCount` | `param.result = 值` 只在返回类型一致时合法。调用点靠 `returnType = int` 过滤才成立 —— 哪天官方改成返回 `long`，这里就会抛 |
| `ConnectionsManager.checkProxy` | 同上，且这个 Hook 点**没有**返回类型过滤 |

两处都补上了 `guard`。这样将来真因签名变化而抛异常，
会直接出现在「运行状态」里，而不是变成一句没有模块前缀的框架日志。

**更值得注意的是另外 5 处：它们必须不包 guard。**

隐身类的三个 Hook（拒绝类加载、拒绝包名查询、拒绝应用信息查询）
**任务就是抛异常给调用方** —— 包上 `guard` 会把异常吞掉，
隐身随之静默失效，**而且完全看不出来**。另外两处（统计上报、在线状态）
的内层 `try-catch` 带有具体语义，套上通用 guard 反而会丢掉线索。

这类"反直觉的正确写法"最容易在后续维护中被人顺手改坏 ——
所以给它们统一加了显式标记：

```kotlin
// 刻意不包 guard：本回调的任务就是抛异常。guard 会把它吞掉，隐身随之静默失效。
```

**把检查做成自动化的。** 光靠这次审计没用，下次加 Hook 还是会漏。
新增 `check_hook_guard.py`，遍历所有 `override fun after/beforeHookedMethod`，
按花括号配对取出函数体，判定三态：

| 判定 | 含义 |
|---|---|
| `guard` | 已防护 |
| `marked` | 有 `刻意不包 guard` 标记，跳过 |
| 未防护 | **违规**，列出位置并给出两种处理方式 |

自带反向测试：构造「违规 / 已防护 / 已标注」三种回调，
确认它抓得出违规、也认得两类合规，**不是只会说"全部通过"的空壳检查**。

至此推送前的静态检查有三项：

```
check_kotlin_imports.py   # 用了但没 import
check_api.py              # 内部工具方法的参数名与结构完整性
check_hook_guard.py       # 回调异常防护（v N1.5 新增）
```

---

### N1.6 —— 反检测自检补全

N1.1 给隐身加了三条新拦截（`Thread.getAllStackTraces`、`printStackTrace` 的取栈路径、
`getApplicationInfo`），N1.2 又把「隐身到底有没有生效」做成了自检 ——
但两边**没有对齐**：自检只覆盖 5 项，新加的拦截里有三条没人验。

后果很具体：**那三条失效时自检照样全绿**。用户看到的是一句
「全部 5 项探测均已挡住」，实际上有三个方向是敞开的。

这一版把自检与拦截面补齐对齐，**从 5 项到 8 项**：

| 新增探测 | 对应拦截 | 检测方会怎么做 |
|---|---|---|
| 全线程堆栈探测 | `Thread.getAllStackTraces()` | 一次拿**所有线程**的栈找框架帧 —— 比逐线程取高效得多，收获也更大 |
| 堆栈打印探测 | `Throwable.getOurStackTrace()` | 把异常打到内存流里再搜关键字，不留痕迹 |
| 已安装应用列表探测 | `getInstalledApplications()` | 与 `getInstalledPackages` 是两条路径，检测方两条都会试 |

**其中「堆栈打印探测」最容易被漏掉。** `printStackTrace` 内部走的是
`Throwable.getOurStackTrace()`，而**公开的 `getStackTrace()` 不经过它** ——
那边另有一份 clone 逻辑。两条路径各自被拦，也就必须各自被验：
只验 `getStackTrace` 那条，`printStackTrace` 就是敞开的，
而检测方恰恰更爱用后者。

至此自检的每一项都能对应到一条实际拦截，报告里不再有
「看起来全绿、实际有缺口」的情况。

---

### N1.7 —— 自检清单与挂载点对齐

N1.6 刚补完反检测那边的「拦截与自检不对齐」，这一版用同一思路查了另一半：
**实际挂载的 Hook 点，有没有都被自检清单覆盖。**

查出两处缺口：

| 挂载点 | 用来做什么 | 此前 |
|---|---|---|
| `MessagesController.updateTimerProc` | 隐藏在线状态 | 没进自检清单 |
| `AndroidUtilities.isTabletInternal` | 强制平板布局 | 没进自检清单 |

后果和 N1.6 那边一模一样：**官方改掉这两个方法名时，对应功能静默失效，
而自检照样全绿**。用户看到「全部命中」，实际上有两项早就不工作了。
两处都补进了自检清单。

**顺带说明为什么平板布局要挂两个方法。** `isTabletInternal()` 内部会把结果
缓存进静态字段，只改 `isTabletForce()` 的话首次调用之后就不再走原来那条路。
两个都是挂载点，就都该被自检盯着。

**把检查也自动化了。** 新增 `check_selfcheck_coverage.py`：从各 `*Hooks.kt`
提取实际挂载的方法名，与 `Diagnostics.kt` 里的自检清单比对，报出差集。
18 个挂载点现已全部覆盖。

同样做了反向测试：把自检清单换成一个只留一项的假文件，
确认脚本能把另外 17 个挂载点全部报出来，且退出码为 1（可直接接进 CI）——
避免它变成只会说"全部通过"的空壳。

至此推送前的静态检查四项：

```
check_kotlin_imports.py        # 用了但没 import
check_api.py                   # 内部工具方法的参数名与结构完整性
check_hook_guard.py            # 回调异常防护         （v N1.5）
check_selfcheck_coverage.py    # 自检清单覆盖完整性   （v N1.7）
```

---

### N1.8 —— 让「功能不可用」一定看得见

前几版在补「两套清单不对齐」，这一版查的是**失败上报**：
功能不可用时，用户到底能不能看到原因。

**审计方法**：扫出所有写着「不可用 / 跳过 / 失效」的日志行，
逐条看它有没有配套的上报动作（`HookStatus.markUnavailable`）。

23 处里有 8 处"没上报"，逐个核实后**真缺口只有 1 处**：
设置页入口在 `fillItems` / `onClick` 挂载不完整时只写了日志 ——
用户看到「入口没出现」，而自检和可用性列表都是干净的，
**没有任何地方说明原因**。另外 7 处是误报（客户端识别走 `profile` 上报、
反检测由隐身自检间接覆盖）。

**顺着这处缺口，发现第二类问题。** 设置页入口还有个失败点是
「找不到设置主页的锚点项」—— 它和前面那些不一样：

> 自检报告是**挂载期算一次就缓存**的，而这个判据要等用户真的打开
> Telegram 设置页、列表被填好之后才能判断。

也就是说，**这类失败永远进不了自检报告** —— 哪怕补上 `markUnavailable`，
也只是躺在日志里（报告早已生成完毕）。

**解法：让不可用项可以"迟到"。**

| 改动 | 作用 |
|---|---|
| `Diagnostics` 记下报告生成时已覆盖的不可用项 | 有了对比基准 |
| 新增 `newlyUnavailable()` | 把之后新增的求差报出来 |
| `DiagBridge` 回传时补在自检段末尾 | 格式与自检项一致，设置界面不用改就能显示 |

这条通路是通用的：以后任何**运行期**才发现的不可用（不只是设置页锚点）
都会自动出现在「运行状态」里，不必再为它单独设计一套上报方式。

---

### N1.9 —— 自检判定与 Hook 定位对齐

到 N1.8 为止，「两套清单」已经查了三轮（统计点、挂载点、拦截面）。
这一版第四轮，查的是**两套判定标准**：

| | 怎么判定「这个 Hook 点能用」 |
|---|---|
| 自检 `Diagnostics.check` | `declaredMethods.filter { it.name == methodName }` —— **只看方法名** |
| `HookFinder.match` | 名字 **+ 返回类型 + 参数个数** |

**有 10 个 Hook 点带着额外过滤条件**（`returnType = int`、
`minParamCount = 3` 这类）。官方改签名时，问题正好落在这条缝里：

> 方法名还在 → 自检显示 ✅ 命中
> 签名对不上 → `HookFinder` 找不到，功能实际不可用

用户于是同时看到「自检说命中」和「可用性说不可用」—— 两边互相矛盾，
而且很容易以为是模块的 bug。

**改法：让自检去问 `HookFinder` 的实际判定。**
`match()` 在精确名单落空时，如果发现名字其实还留在类里（只是签名不符），
就单独记一笔；自检据此把该项标红，并说明
「方法名还在，但没有一个签名满足过滤条件」。

这一项与「靠特征兜底命中」**分开报** —— 那个还能工作，这个已经不能了，
是两回事。

**没有任何行为改变**（Hook 逻辑一行没动），只是把两个本来不一致的判定标准
对齐，让自检不再给出误导性的安心感。

---

### N1.10 —— 自检移出启动关键路径

这一版修的是**我自己在 N1.6 引入的性能问题**。

N1.6 给反检测自检补了三项探测，其中一项是 `Thread.getAllStackTraces()` ——
它会把**所有线程**挂起、逐个取栈，比前面几项贵得多。

问题不在探测本身，而在它跑的位置：自检是在 `handleLoadPackage` 里
**同步执行**的，那段代码的耗时**直接算进 Telegram 的启动时间**。
也就是说，为了让「隐身是否生效」可验证，每次冷启动都要付一次挂起全线程的代价。

**改法：把自检移出启动路径。**

| | 之前 | 现在 |
|---|---|---|
| 时机 | `handleLoadPackage` 内同步执行 | `Application.onCreate` 之后 1.5 秒 |
| 线程 | 主线程（启动关键路径） | 后台线程 `TgEnhance-diag` |

选 1.5 秒的原因：那一刻应用刚起来、主线程正忙，不该凑上去；
但也不能太久 —— 用户可能在启动后很快切到模块设置界面看「运行状态」，
太晚首屏就看不到自检结果。

**统计输出一并挪过去了**（它本来就要积累一段时间才有意义），
两件事合并成一个「启动后任务」，时机判断只留在一处。

**对使用没有影响**：自检结果照样进「运行状态」，只是生成时间从
「挂载那一刻」变成「启动完成后一点点」。万一切得太快、报告还没生成，
界面会显示「未执行自检」，而不是一片空白。

---

### N1.11 —— 功能表补齐

查第 6 轮「两套」：**README 的功能列表 vs 注册表里实际有的项**。

结果是 README 漏了 **6 个真实功能**：

| 漏掉的项 | 所属分组 |
|---|---|
| 禁用自动播放 | 网络 |
| 隐藏对方的在线状态 | 隐私 |
| 隐藏对方的最后上线时间 | 隐私 |
| 隐藏手机号 | 隐私 |
| 强制平板布局 | 高级 |
| 关闭更新提示 | 高级 |

前面十版一直在加功能、加检查，但**给别人看的那份列表没跟上** ——
只看 README 的人不会知道模块还能做这些。

顺带把分组顺序与设置界面对齐（README 原本把「广告屏蔽」排在「隐私」前面，
而实现里是反过来的），并补上原本缺失的「高级功能」一节。

现在功能表与设置界面里的分组一一对应。

---

### N1.12 —— 死代码清理与一处未兑现的承诺

第 7 轮「两套」检查：**声明了的成员 vs 实际被用到的**。

做法是遍历所有 `object` 的公开成员，统计带前缀引用次数。
第一版判定写错了（把「只被调用一次」也当成死代码，一次报了 71 处），
修正成「完全没被引用」后剩 8 处 —— 逐个核实，5 处是误报
（在 `summary()` 里用、被同文件派生、脚本没覆盖 `if (x)` 这类写法）。

**真正的收获只有一条。**

N1.1 写 `LauncherIcon.apply()` 时留了句话：

> 每次设置页启动时都会调一次，这样即使状态因为换机、刷机、恢复备份而错位，
> 也能自动纠回来 —— 否则会出现「开关显示开着、图标其实还在」这种自相矛盾的状态。

`isHiddenNow()` 就是为了兑现这句话写的 —— 然后**忘了调用**。
也就是说，那个「对账」行为其实**一直没发生过**。

现在补上了：`apply()` 在动手之前先读一次系统里的实际状态，
不一致时记一行日志再纠正。用户从系统设置、LSPosed 管理器或别的方式
改过组件状态时，「开关失灵」这类现象至少有了可查的线索。

**顺带清理的死代码：**

| 位置 | 情况 | 处理 |
|---|---|---|
| `XLog.enabled` | 定义了「完全关闭日志」的开关，但**从来没有任何地方设置过它** | 删除 —— `verbose` 已经够用，留着只会让人以为它有效 |
| `HookFinder.readableSignature()` | 定义了没人调 | 删除 |
| `StealthSelfTest.render()` | 与 `Diagnostics.stealthSelfTest()` 功能重复 | 删除 |
| `Features.groupRootKeys` | 注释说用于启动日志，实际各 Hook 自己打印 | 删除 |
| `ConflictWatch.size` | 与 `list().size` 重复 | 删除 |
| `OsCompat.isNewerThanKnown()` | 注释写着「只在日志里提一句」，但没接上 | **接上** —— 比已知上限还新的系统会在诊断报告里标出来 |
| `HookStats.activeCount()` | 注释说用于汇总，汇总里却没用 | **用起来** —— 统计输出加「其中 N 项已触发」 |

原则是：**能接上的接上，接不上的删掉**。留一个没人用的成员，
下一个人会以为它在某处起作用。

---

### N1.13 —— 设置页入口改成原生样式

N1.4 把入口插进了 Telegram 设置页，用的是 `UItem.asSettingsCell` —— 纯文字行。
可设置页里**每一行都带左侧彩色圆角图标块**，我们那行混在里面明显「少一块」。

**先把类找对。** 之前试着下载 `org/telegram/ui/SettingCell.java` 一直 404，
直到列出 `ui/` 目录（231 个文件）才发现：**`SettingCell` 根本不是独立文件**，
它是 `SettingsActivity` 的**内部静态类**，全名
`org.telegram.ui.SettingsActivity$SettingCell`。按独立文件找当然找不到。

拿到签名后：

```java
public static UItem of(int id, int iconColorTop, int iconColorBottom,
                       int icon, CharSequence title, CharSequence subtitle)
```

**改法：优先走原生那条路，失败再退回。**

| 路径 | 效果 |
|---|---|
| `SettingCell.Factory.of(...)` | 与 Telegram 自己的设置项**同款**：彩色圆角图标块 + 标题 + 副标题 |
| `UItem.asSettingsCell(...)`（退回） | 纯文字行 |

第 2 条不是凑数的备用方案：`SettingCell` 是内部类，fork 把它搬走、
或者改掉 `of` 的签名都是可能的 —— 那种情况下能显示一行可点的文字，
也比完全没有入口强。

**顺手解决图标从哪来。** 不能引用本模块的图标资源 —— 资源 id 是**按包独立**的，
拿到宿主进程里毫无意义。改为按名字查宿主自己的 drawable
（`settings_features` → `settings_ask` → `settings_faq` 依次尝试），
取到后缓存（`getIdentifier` 是查表操作，不该放高频路径）。
底色用 Telegram 自己的 `IconBackgroundColors.BLUE` 色值，
正好与设置页第一项「账号」同色。

都取不到也不影响使用：条目照常显示、照常可点，只是没有图标块。

---

### N1.14 —— Hook 点签名逐条核对

这是最后一大类没系统验过的风险：**项目里的过滤条件，与 Telegram 源码里的
真实签名到底对不对得上。**

为什么要专门核：`HookFinder` 定位 Hook 点时会带 `returnType`、`paramCount`
这类过滤条件。条件与真实签名不符时，方法名还在、自检也会绿，
但 `HookFinder` **永远找不到它** —— N1.9 修的就是这个矛盾的另一半。

**做法**：把源码拉下来逐条比对 16 个 Hook 点。
`MessagesController` 有 1.26 MB，直连下载三次都断流，
改用 GitHub 的 **blob 接口**取 base64 才拿到完整文件。

**结论：全部一致。** 这一轮在技术上「空手而归」，但值得 ——
它是唯一一类**静态检查查不出、只有实机才会暴露**的风险，现在排除了。

核对中有两处值得单独记下：

**1. `checkProxy` 返回 `long`。** 项目里写的是 `param.result = 0L`，正好匹配。
若当初写成 `0`（`Int`）就会抛 `ClassCastException` —— 这种错只在真机上炸，
`checkKotlinImports` / `check_api` 一个都拦不住。

**2. `completeReadTask` 是 private 方法。** 拿「public 方法」当条件去搜会一直
找不到它，一度以为被改名了。实际上项目的 `hookAllByName` 是按名字挂载所有
重载、**不看可见性**，所以挂得上。

顺带把上面那张「Hook 点依据」表补全了 —— 原先只列了 6 项，实际有 16 个；
现在每行都带源码行号、过滤条件与核对结果。下次 Telegram 升级要重新核对时，
照着这张表 diff 就行。

---

### N1.15 —— Android 16/17 适配调研 + insets 走现代 API

**先说结论：Android 16 的变更不对本模块生效。** 但这个「不生效」是查出来的，不是猜的。

Android 16（API 36）有几条知名变更，逐条对照本模块：

| 变更 | 影响本模块？ | 为什么 |
|---|---|---|
| edge-to-edge 不能再退出 | ❌ | 只对 `targetSdk 36` 生效；本模块是 35，且已自行处理 insets |
| 预测性返回默认启用（`onBackPressed` 不再被调用） | ❌ | 同上；而且本模块**没有重写** `onBackPressed` |
| 有序广播优先级不再跨进程 | ❌ | 只影响 `sendOrderedBroadcast`；本模块诊断回传用普通 `sendBroadcast` |
| Intent 重定向保护 | ❌ | 本模块跳转一律带 `setPackage`，本来就不走重定向 |
| 16KB page size | ❌ | 影响 native 库；本模块纯 Kotlin，包里没有 `.so` |

调研结论写进了 `OsCompat` 的注释里 —— 下次有人问「支持 Android 16 吗」，
不用再查一遍。同时标注了一件事：**真要升 `targetSdk` 到 36，前两条需要重新评估。**

**顺手改掉一处过时写法。** 设置页的 insets 处理原来用
`insets.systemWindowInsetTop` 那一组 —— 它们**在 API 30 就已废弃**
（现在还能用，但将来某个版本会移除）。改用 `WindowInsets.Type`：

```kotlin
insets.getInsets(WindowInsets.Type.systemBars() or WindowInsets.Type.displayCutout())
```

顺带修掉一个被掩盖的问题：`systemBars` 与 `displayCutout` 在**挖孔屏上不是同一块区域**，
只取前者的话横屏时内容仍可能被挖孔盖住。旧 API 恰好把两者混在一起，
所以这个隐患一直没暴露。API 30 以下保留旧分支（`minSdk = 26`）。

---

### N1.16 —— 编译目标升到 Android 16（API 36）

`compileSdk` 从 35 升到 36。**但 `targetSdk` 刻意停在 35** —— 这两件事要分开看：

| | 作用 | 本次 |
|---|---|---|
| `compileSdk` | 编译期能用哪些 API | 35 → **36** |
| `targetSdk` | 运行期按哪一版的规则对待本应用 | 保持 **35** |

升 `compileSdk` 是纯收益：能引用 API 36 的符号，也让「支持到 Android 16」有依据。
升 `targetSdk` 则会一次性引入两条行为变更（edge-to-edge 不能再退出、
预测性返回强制启用）—— 技术上本模块扛得住（没重写 `onBackPressed`、
也自行处理了 insets），但**没有收益**，只是多两个要照顾的分支。
所以留在 35，并在 `build.gradle.kts` 里写明了「什么时候该升」。

**这中间撞上一条版本链。** 一开始只改了 `compileSdk`，随后发现：

> `compileSdk = 36` 要求 **AGP ≥ 8.9.0**（更早的 AGP 压根不知道 Android 16 存在）
> → 而 AGP 8.9 又要求 **Gradle ≥ 8.11.1**

项目原本是 AGP 8.7.3 + Gradle 8.9，两个都不够。这是**链式的，缺一环构建就停**。
于是三个一起动：

| 组件 | 原 | 新 |
|---|---|---|
| `compileSdk` | 35 | 36 |
| AGP | 8.7.3 | 8.9.1 |
| Gradle（CI） | 8.9 | 8.11.1 |

CI 里安装的 SDK 组件同步换成 `platforms;android-36` + `build-tools;36.0.0`
（推之前先查过 Google 的 SDK 仓库清单，确认这两个包确实存在，免得白跑一轮 CI）。

升 AGP 的常见副作用也提前排查了：本模块**不用 `BuildConfig`**，
而 AGP 8.x 起该特性默认关闭 —— 不受影响。

---

### N1.17 —— 把零散检查固化成脚本

前十几版做过的审计里，有三项一直是**临时敲命令**查的（统计点一致性、
上报 key 有效性、开关是否被读）。每次发版都要重敲一遍，很容易漏。
这一版把它们固化成脚本，加上原有的检查，**发版前跑几条命令就够了**：

| 脚本 | 查什么 |
|---|---|
| `check_kotlin_imports.py` | 用了但没 import |
| `check_api.py` | 内部工具方法的参数名与结构完整性 |
| `check_hook_guard.py` | 回调异常防护（N1.5） |
| `check_selfcheck_coverage.py` | 自检清单覆盖完整性（N1.7） |
| `check_registry_consistency.py` | **注册表引用一致性（N1.17 新增）** |

新增这个脚本一次查三项 —— 它们的共同点是**靠字符串 key 互相引用，
写错了不会有编译错误，只表现为「功能静默不对」**：

| 检查 | 对不上时的症状 |
|---|---|
| `HookCatalog` 声明 vs `HookStats.hit()` 打点 | 声明了不打点 → 界面永远显示 0；打点没声明 → 界面显示原始 key |
| `markUnavailable` 的 key 是否在功能注册表里 | 界面查不到标题，只能显示配置 key |
| 注册表的布尔开关是否真被 hook 端读过 | **从没读过的开关就是摆设** —— 能拨，但拨了没效果 |

第三项需要一份例外白名单：只作用于模块界面的开关（如「隐藏桌面图标」）
hook 端本就不该读，排除掉才不会误报。

**照例做了反向测试**：造一个「声明了不打点 + 用了不存在的 key +
有没人读的开关」的假项目，三项全部被抓出、退出码为 1 ——
确认它不是只会说「全部通过」。

**全量回归结果**：五项检查全绿 —— 统计点 31/31 双向对齐、
上报 key 13 个全部有效、24 个开关都被读取、25 个回调都有防护或已标注、
18 个挂载点都被自检覆盖。

---

## N2 线

### N2.0 —— Hook 点注册表

N2 开篇不做新功能，先还一笔债。

**这笔债是什么。** N1 期间反复踩同一个坑：**同一条信息被两处各自维护**，
改了一处忘了另一处，不会报错，只会「功能静默不对」：

- **N1.7**：`updateTimerProc`、`isTabletInternal` 两个挂载点从没进过自检清单 ——
  它们改名时功能会默默失效，而自检照样全绿；
- **N1.9**：自检只看「方法名在不在」，而 `HookFinder` 还要看返回类型与参数个数 ——
  官方改签名时自检说「命中」、功能说「不可用」，两边互相矛盾。

两轮都是**打补丁**：把漏的补进去、把判定对齐。但根没动 ——
「怎么找到这个 Hook 点」仍然散在各 `*Hooks.kt` 的 `match(...)` 调用里，
而自检另有一份手写清单。

**N2.0 把根解决：建一张 `HookRegistry`。** 每个 Hook 点的定位信息收拢到一处：

| 声明项 | 说明 |
|---|---|
| `key` | 唯一标识，与 `HookCatalog` 的统计 key 对应 |
| `label` | 面向用户的中文名（自检报告里显示） |
| `owner` | 类从哪来：写死 / 主 Activity / StoriesController / 设置页 |
| `names` | 精确方法名名单 |
| `returnType` / `paramCount` 等 | 过滤条件 |
| `featureKey` | 对应哪个功能开关（「当前客户端不支持」时回显用） |

自检从此**从注册表派生** —— 注册表里有什么就查什么，
「加了挂载点却漏了自检」在结构上不可能再发生。

**顺带解决的几件事：**

- 类名不再散在 `run()` 里对每个 profile 字段各写一遍，
  收敛成一个 `resolveClass(target, loader)`；
- **一个挂载点驱动多个计数**的情况被显式声明出来
  （`formatUserStatus` 同时驱动「隐藏对方在线」与「隐藏最后上线」）；
- **不产生计数的挂载点**也标了出来 —— 有几处用 `XC_MethodReplacement`
  直接替换原实现，没有「被调用」的概念，计数无从谈起。
  它们依然要被自检盯着，所以留在注册表里。

**新增一条运行期自检**：注册表与统计表的一致性。它直接比较两张表的真实内容
（不是文本匹配），对不上时在「运行状态」里点名 —— 而不是等某天发现某行永远是 0。

**验证**：注册表 20 个条目（2 个不计数）导出 21 个计数点，
`HookCatalog` 声明 31 个、豁免 10 个（隐身 9 + 回调异常 1）——
**21 + 10 = 31，严丝合缝**。

> 各 `*Hooks.kt` 里的挂载调用暂时仍用自己的 `match(...)` 参数 ——
> 把它们也改成从注册表读放在 N2.1。这一步先让**自检**这一侧彻底对齐，
> 风险小、收益立竿见影。

---

### N2.1 —— 挂载点也走注册表

N2.0 让**自检**从注册表派生，这一版把**挂载**也接上。

各 `*Hooks.kt` 里原本写着一长串定位参数：

```kotlin
HookFinder.match(
    cls,
    explicitNames = listOf("sendTyping"),
    returnType = Boolean::class.javaPrimitiveType,
    nameContains = "typing",
    minParamCount = 3
)
```

现在统一成一行：

```kotlin
HookFinder.matchByKey(cls, "privacy.typing")
```

**11 处全部改完**，条件只在注册表里存在一份。「注册表里写的」与「挂载时用的」
从此不可能不一致 —— 这正是 N2.0 建注册表要解决的问题。

新增的 `HookFinder.matchByKey(cls, key)` 在找不到 key 时**返回空列表**而不是抛异常：
调用方本来就有一条「定位不到 → 标记功能不可用」的降级路径，走那条比让整个挂载组挂掉安全得多。

**顺手清理**：`ThemeHooks` 里的 `STORIES_QUERY_NAMES` 常量随之下线（内容已进注册表）。

**三处暂未统一，都写清了原因** —— 不是遗漏，是条件确实比注册表现在能表达的更细：

| 位置 | 为什么 |
|---|---|
| `canDownloadMedia` | 还要按**参数类型**过滤（只取 `MessageObject` 那个重载，另一个 `(int, long)` 是预设判断，不在功能范围内） |
| `AccountInstance.getInstance` | 还要要求**静态**、且参数是 `int` |
| `HookEntry` 的 `onResume/onPause` | 用 `hookAllByName` 按名挂载，本身就是「挂全部重载」的语义 |

**检查脚本随之改造。** 原来那个「自检清单覆盖检查」的意义变了 ——
挂载与自检都来自注册表，天然一致。改成查**有没有人绕过注册表**：

- 有没有残留的裸 `HookFinder.match(...)` 调用（应为零）；
- `matchByKey` 用到的 key 是否都真实存在（拼错会安静失效，功能没了却不报错）；
- 反向提示：注册表里声明了却既没按 key 定位、也没有同名 `hookAllByName` 的 key。

---

## 常见问题

**「运行状态」里某项显示「未触发」，是坏了吗？**
不一定。它只代表这段时间没出现过对应动作（例如没人撤回消息）。
真正要怀疑的是：你明确做了对应操作（比如自己删了一条「为所有人删除」的消息），
计数仍然是 0 —— 那才是 Hook 点随版本失效。

**自检显示某个 Hook 点没匹配上？**
先看它给出的「相关项」候选，多半是官方改了方法名，一眼就能对上。

**支持哪些客户端？**
只要加载 `org.telegram.messenger.UserConfig` 的客户端都支持，包括改过包名的第三方 fork。
Telegram X 是另一套架构，不支持。

**能隐藏 root 吗？**
不能，而且**不建议**在本模块里做。

root 隐藏是**全局需求** —— 银行、支付类应用都会检测，而本模块只在 Telegram
进程内运行。在这里做只能骗过 Telegram 一个应用，其它应用照样检测得到。

正确做法是用系统层的隐藏方案：KernelSU / Magisk 生态里的 **Shamiko**、
**Zygisk Assistant** 之类。它们在 Zygote 层就把 root 痕迹卸掉，
覆盖全部应用，也比在单个 App 里 hook 文件读取彻底得多。

本模块的边界是**只处理自己的痕迹**（见「反检测」一节）：不伪造 `Build` 属性、
不隐藏 root、不动 `/proc/self/maps`。那些属于全局环境改造，出错会波及整机，
不该由一个 Telegram 增强模块代劳。

**能解除 +86 风控、或免费解锁 Premium 吗？**
技术上做不到，本模块也不做。

这两类判断都在 Telegram **服务器侧**：区号相关的账号限制、账号信誉、
会员权益，全部由后端根据你的账号状态决定。客户端只是个渲染层 ——
把提示文字改掉，服务器该拒绝还是会拒绝，只是让错误更难理解。

至于免费解锁 Premium，那属于绕过付费，是本项目明确不碰的范围
（见「合规边界」）。本模块只做本地行为调整与显示层面的增强。

**怎么反馈问题？**
设置界面 →「运行状态」→「分享诊断报告」，把导出的文本发出来即可，
已包含版本、配置与自检结果。

## 参考与致谢

本项目是**独立实现**，不是任何项目的分支，也没有复制它们的代码。
功能方向上参考了以下开源模块（均为 GPL-3.0）：

| 项目 | 作者 | 本项目参考了什么 |
|---|---|---|
| [TeleVip-LSPosed](https://github.com/mustafa1dev/TeleVip-Lsposed) | mustafa1dev | 隐私功能的分类方式（已读 / 输入状态 / 在线状态），以及「多客户端适配」这件事必须认真做 |
| [Telegami](https://github.com/aoya111/Telegami) | aoya111 | 同为 Kotlin 重写，印证了针对 fork 客户端做类路径探测的必要性 |
| [NoAdsTelegram](https://modules.lsposed.org/module/ai.noads.NoAdsTelegram) | — | 「按类别分组设置项」的界面组织方式 |

### 与它们刻意划开的边界

| 它们做的事 | 本模块 |
|---|---|
| 伪造 Telegram Premium、本地解锁会员功能 | **不做** —— 不伪造会员状态 |
| 去除赞助消息 / 广告 | 做，但**默认关闭**、开启前说明影响 —— 本模块唯一一项与收入相关的功能 |
| 绕过内容保存 / 转发限制 | **不做** —— 属于绕过内容保护 |
| 阻止阅后即焚媒体被删除 | **不做** —— 保留他人以为会消失的内容，伦理上不该由本模块代劳 |
| 隐藏手机号 | 做，但用**遮罩**保留末 4 位，而不是直接抹掉 |
| 隐藏 Stories / 输入状态 / 在线状态、防撤回 | 做 |

如果只想要「破解会员」这类功能，请看上面那几个项目 ——
本模块的定位是**本地体验增强**，不涉及任何商业化能力的绕过。

另外：这些模块都只适配 README 里列出的**特定客户端版本**，换版本就可能失效。
本模块在这方面的做法不同 —— 用候选类路径与特征匹配来扩大适配面，
并在自检里明确告知「哪些功能在当前客户端不可用」，而不是让用户自己试。

---

## 免责声明

本项目仅供个人学习与自用。使用本模块产生的任何后果由使用者自行承担。请遵守所在地区的法律法规以及 Telegram 的服务条款。
